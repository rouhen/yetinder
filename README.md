# Welcome to Yetinder!
## How to start
- Install Composer dependencies:
  ```bash
  composer install
  ```
- Start Docker:
  ```bash
  docker compose up
  ```
- Open your browser and go to:
  ```
  http://localhost:8080
  ```
